{
  "name": "milestone-project-management-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:client": "npm install --prefix \"frontend - client\"",
    "dev:client": "npm run dev --prefix \"frontend - client\"",
    "build:client": "npm run build --prefix \"frontend - client\"",
    "dev": "npm run dev --prefix \"frontend - client\""
  }
}
